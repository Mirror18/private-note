#git
```bash
#处理密钥
ssh-keygen -t ed25519 -f ~/.ssh/github_mirror -C "mirrorcloud3@gmail.com"
 C:\Users\mirror/.ssh/id_rsa
 ssh-keygen -t ed25519 -f C:\Users\mirror/.ssh/github_mirror -C "mirrorcloud3@gmail.com"
```

在 `.ssh` 下新建 `coonfig` 文件，保存以下文本
```config
Host github.com
	HostName github.com
	User git
	IdentityFile C:\Users\mirror/.ssh/github_mirror
```

